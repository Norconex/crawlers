<?xml version="1.0" encoding="UTF-8"?>
<hazelcast xmlns="http://www.hazelcast.com/schema/config"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://www.hazelcast.com/schema/config
           http://www.hazelcast.com/schema/config/hazelcast-config-5.3.xsd">

    <cluster-name>nx-crawler-cluster</cluster-name>
    
    <!-- Default network configuration -->
    <network>
        <port auto-increment="true">5701</port>
        <join>
            <!-- Auto-detect other cluster members on the same network -->
            <multicast enabled="true"/>
            <tcp-ip enabled="false"/>
        </join>
    </network>
    
    <!-- Map configuration for caches -->
    <map name="nx-cache-*">
        <!-- Default TTL for cache entries: 1 hour -->
        <time-to-live-seconds>3600</time-to-live-seconds>
        <eviction size="10000" max-size-policy="PER_NODE" eviction-policy="LRU"/>
        <backup-count>1</backup-count>
    </map>
    
    <!-- Executor service configuration -->
    <executor-service name="nx-task-executor">
        <pool-size>16</pool-size>
        <queue-capacity>1000</queue-capacity>
    </executor-service>
    
    <!-- CP Subsystem configuration for locks and counters -->
    <cp-subsystem>
        <cp-member-count>3</cp-member-count>
        <semaphores>
            <semaphore>
                <name>nx-lock-*</name>
                <initial-permits>1</initial-permits>
            </semaphore>
        </semaphores>
        <atomic-longs>
            <atomic-long>
                <name>nx-counter-*</name>
                <initial-value>0</initial-value>
            </atomic-long>
        </atomic-longs>
    </cp-subsystem>
</hazelcast>