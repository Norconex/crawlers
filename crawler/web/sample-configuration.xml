<blah>

<!-- Close to V3: -->

<configuration id="acme-crawler-config">
  <crawlerDefaults>
  </crawlerDefaults>
  <crawlers>
    <crawler id="acme-crawler-a">
    </crawler>
    <crawler id="acme-crawler-b">
    </crawler>
    <crawler id="acme-crawler-c">
    </crawler>
  </crawlers>
</configuration>

<!-- OR -->
<crawl-session>
  <defaults>
  </defaults>
  <instances>
    <instance id="acme-crawler-a">
    </instance>
    <instance id="acme-crawler-b">
    </instance>
    <instance id="acme-crawler-c">
    </instance>
  </instances>
</crawl-session>




<!-- 

V4 concepts:

1 crawl session can launch 1 or multiple crawler instances (same JVM)
   - a session as a start and an end (start URLs/no more URLs to crawl).
   - each doc crawled at most once per session
   - a session is equivalent to a "job" or Kafka "task". 
 


 -->
 
 
 
</blah>
