---
component: Browser
module: shared
locale: en
propertyCoverage: all
---

# Browser

## Summary

Supported browser types for WebDriver-based fetching.

## Notes

Specifies the web browser to use when fetching pages with a WebDriver-based fetcher. Supported values correspond to browsers that can be automated via WebDriver (e.g., Chrome, Firefox).

## Properties
