---
component: ScriptTransformer
module: importer
locale: en
propertyCoverage: all
---

# ScriptTransformer

## Summary

Transformer incoming documents using a scripting language.

## Notes

The default script engine is JavaScript.

Refer to [ScriptRunner](https://opensource.norconex.com/importer/v3/apidocs/com/norconex/importer/handler/ScriptRunner.html) for more information on using a scripting language with Norconex Importer.

### How to tag documents with scripting:
The following are variables made available to your script for each document:

- `reference`: Document unique reference as a string.
- `content`: Document content, as a string (of maxReadSize length).
- `metadata`: Document metadata as an [Properties](https://opensource.norconex.com/commons/lang/v2/apidocs/com/norconex/commons/lang/map/Properties.html?is-external=true) object.
- `parsed`: Whether the document was already parsed, as a boolean.
- `sectionIndex`: Content section index if it had to be split, as an integer.
There are no expected return value from your script. Returning one has no effect.

**TODO: rewrite it as lua do not support in some case, and some issue in js(recall in DnD)**

## Properties

### engineName

TODO: Add documentation for this property.

### fieldMatcher

Matches document fields based on specified patterns and methods

### maxReadSize

Max characters to read at once

### script

The script content or path to execute as part of this handler's processing logic.

### sourceCharset

Sets the character encoding of the source document to ensure proper reading and processing.

## Examples

The following examples add new metadata field indicating which fruit is the document about.

**JavaScript**

<ConfigBlock
  json={{
    "handler": {
      "class": "ScriptTransformer",
      "script": "<![CDATA[metadata.add('fruit', 'apple');]]"
    }
  }}
/>

### lua

***LUA***

<ConfigBlock
  json={{
    "handler": {
      "class": "ScriptTransformer",
      "engineName": "LUA",
      "script": "<![CDATA[metadata:addString('fruit', {'apple'});]]>"
    }
  }}
/>
