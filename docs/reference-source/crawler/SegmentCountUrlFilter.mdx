---
component: SegmentCountUrlFilter
module: crawler/web
locale: en
propertyCoverage: all
---

# SegmentCountUrlFilter

## Summary

Filter by the number of reference segments.

## Notes

TODO: Add notes.

## Properties

### count

Minimum number of segments.

### duplicate

Use this option to count the number of duplicate segments instead.

### onMatch

What to do with a reference once its segment count has been reached.

### separator

Regular expression matching an alternate segment separator (default is: /)

## Examples

<ReferenceExamples
  examples={[
    {"title":"default","description":"The above example will reject URLs with more than 5 forward slashes after the domain.","codeByLanguage":{"json":"{\n  \"class\": \"SegmentCountUrlFilter\",\n  \"onMatch\": \"EXCLUDE\",\n  \"count\": 5\n}","yaml":"class: SegmentCountUrlFilter\nonMatch: EXCLUDE\ncount: 5","xml":"<class>SegmentCountUrlFilter</class>\n<onMatch>EXCLUDE</onMatch>\n<count>5</count>"}}
  ]}
/>
