---
component: LocalFetcher
module: crawler/fs
locale: en
propertyCoverage: all
---

# LocalFetcher

## Summary

Local file system fetcher.

## Notes

Fetches files from the local file system. Supports optional credentials and access control list (ACL) configuration for restricted file paths.

## Properties

### aclDisabled

When enabled, Access Control List (ACL) permissions are not applied or forwarded to the target system.

### credentials

Username and password when required by the local file system.

### domain

The domain name for authentication when accessing the remote resource.

### referenceFilters

Filters applied to document references (URLs or file paths) to control which ones are fetched. Documents whose references do not pass the filters are skipped.
