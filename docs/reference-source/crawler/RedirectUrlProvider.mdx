---
component: RedirectUrlProvider
module: crawler/web
locale: en
propertyCoverage: all
---

# RedirectUrlProvider

## Summary

Resolves the target URL from HTTP redirect responses.

## Notes

Provides the resolved redirect URL when the crawler encounters an HTTP redirect response. Select one of the supported implementations to configure redirect URL resolution behavior.

## Properties
