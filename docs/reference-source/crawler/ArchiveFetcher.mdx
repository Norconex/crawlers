---
component: ArchiveFetcher
module: crawler/fs
locale: en
propertyCoverage: all
---

# ArchiveFetcher

## Summary

Fetches documents contained within archive files (ZIP, TAR, etc.).

## Notes

Fetches documents from archive files (such as ZIP or TAR) on the local file system. Supports credential configuration and reference-level filtering.

## Properties

### credentials

TODO: Add documentation for this property.

### domain

The domain name for authentication when accessing the remote resource.

### referenceFilters

Filters applied to document references (URLs or file paths) to control which ones are fetched. Documents whose references do not pass the filters are skipped.
